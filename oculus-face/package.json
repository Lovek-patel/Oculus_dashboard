{
  "name": "oculus-face",
  "private": true,
  "version": "1.0.0",
  "description": "Ambient projector dashboard — pulls Google Calendar + Tasks into a static data.json for the display.",
  "scripts": {
    "sync": "node scripts/sync.js",
    "get-token": "node scripts/get-refresh-token.js"
  },
  "dependencies": {
    "googleapis": "^140.0.0"
  }
}
